{
  "name": "origination-platform",
  "version": "0.1.0",
  "description": "REST API for a bank origination platform, built on entity-engine + workflow-engine.",
  "main": "dist/index.js",
  "scripts": {
    "setup": "npm --prefix ../entity-engine install && npm --prefix ../entity-engine run build && npm --prefix ../workflow-engine install && npm --prefix ../workflow-engine run build && npm install",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "entity-engine": "file:../entity-engine",
    "workflow-engine": "file:../workflow-engine",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.0",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
